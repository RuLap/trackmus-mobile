import { View, Text } from "react-native";

export default function TasksPage() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 20 }}>Your tasks will be here 🎶</Text>
    </View>
  );
}
